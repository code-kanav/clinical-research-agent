#!/usr/bin/env python3
"""Root-level entry point. Install with `pip install -e .` then use `clinical-research` instead."""
from clinical_research_agent._cli import main

if __name__ == "__main__":
    main()
